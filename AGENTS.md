# AGENTS.md — FTP Browser

> 아키텍처 상세, 캐시 DB 스키마, 코딩 컨벤션, 디렉토리 구조 → [CLAUDE.md](./CLAUDE.md)

---

## 프로젝트 요약

FTP 서버 탐색 시 **이미지 썸네일 미리보기**를 제공하는 데스크톱 앱.
FTP 프로토콜에 썸네일 API가 없으므로 **캐시 기반 썸네일 시스템**을 자체 구현한다.

---

## 기능 요구사항

### 🔴 필수
- FTP 서버 연결/해제, 디렉토리 탐색, 파일 업로드/다운로드/삭제
- 로컬 파일시스템 탐색 및 파일 선택
- 이미지 썸네일 표시 (JPG, PNG, GIF, BMP, WebP, TIFF)
- 썸네일 로컬 캐시 시스템
- 리스트 뷰 ↔ 그리드 뷰 전환, Lazy Loading

### 🟡 권장
- 서버 목록 관리, 연결 상태 표시
- 파일 이름 변경, 디렉토리 생성, 파일 정보 표시
- 전송 큐/진행률, 드래그 & 드롭, 경로 직접 입력
- 썸네일 클릭 시 큰 미리보기, 캐시 관리 UI

### 🟢 선택
- FTPS/SFTP 지원, 다중 서버 탭, 썸네일 크기 조절

---

## 썸네일 캐시 — 핵심 전략

**흐름**: 디렉토리 진입 → 이미지 필터링 → 캐시 HIT면 즉시 표시 / MISS면 백그라운드 다운로드 → 썸네일 생성 → 캐시 저장 → UI 표시

**캐시 키**: `hash(호스트 + 포트 + 원격경로 + 파일크기 + 수정시간)`

**최적화**: 뷰포트 기반 로딩, 우선순위 큐, 동시 다운로드 제한(3개), EXIF 썸네일 우선 추출, LRU 캐시 정리

---

## 기술 스택 (미확정 — 구현 전 협의 필수)

| 옵션 | 구성                  | 특징                         |
| ---- | --------------------- | ---------------------------- |
| A    | Electron + React + TS | 풍부한 생태계, 크로스 플랫폼 |
| B    | Tauri + React + TS    | 경량(Rust), 높은 성능        |
| C    | Python (PySide6)      | 빠른 프로토타이핑            |

---

## 로드맵

1. **기반 구축** — 스택 확정, FTP 연결/해제, 디렉토리 목록
2. **핵심 FTP** — 업로드/다운로드/삭제, 로컬 탐색기, UI 레이아웃
3. **썸네일 시스템** — 캐시 매니저, 썸네일 생성, 백그라운드 큐, Lazy Loading
4. **완성도** — 서버 관리, 전송 큐, 설정, 에러 처리

---

## 미결 사항

- 부분 다운로드(`REST`) 활용 가능 여부
- 썸네일/캐시 기본 크기 (150x150? / 500MB?)
- 프로토콜 범위 (FTP / FTPS / SFTP)
- 대용량 이미지(50MB+) 처리 정책

---

## 참조

- [CLAUDE.md](./CLAUDE.md) — 아키텍처 상세, 코딩 컨벤션, 기술 결정 기록
- [RFC 959](https://datatracker.ietf.org/doc/html/rfc959) — FTP 프로토콜
- [RFC 4217](https://datatracker.ietf.org/doc/html/rfc4217) — FTP over TLS
